type Task {
        id: ID! @id
        description: String!
        taskName: String!
        postedBy: User

    }

    type User {
        id: ID! @id
        name:String!
        email:String! @unique
        password: String!
        tasks: [Task!]!
    }

